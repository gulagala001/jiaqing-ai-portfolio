const heroTabs=[...document.querySelectorAll('[data-project-tab]')];
const projectInfo={trisoul:{title:'TriSoul',kind:'机制图解',copy:'独立生成、匿名互评，再把分歧带回执行。',href:'projects/trisoul.html'},penecho:{title:'PenEcho',kind:'结构图解',copy:'让手写、项目记忆与设备使用延续起来。',href:'projects/penecho.html'},voxelbench:{title:'VoxelBench',kind:'被测作品 · 模型生成',copy:'生成第一版之后，能否继续交付？',href:'projects/voxelbench.html'}};
function activateHero(tab){const id=tab.dataset.projectTab;for(const b of heroTabs){b.setAttribute('aria-selected',String(b===tab));b.tabIndex=b===tab?0:-1;}for(const panel of document.querySelectorAll('[data-project-panel]'))panel.hidden=panel.dataset.projectPanel!==id;const info=projectInfo[id];document.querySelector('#hero-visual-kind').textContent=info.kind;document.querySelector('#hero-project-title').textContent=info.title;document.querySelector('#hero-project-copy').textContent=info.copy;const link=document.querySelector('#hero-project-link');link.href=info.href;link.setAttribute('aria-label','查看 '+info.title+' 案例');}
heroTabs.forEach((tab,i)=>{tab.addEventListener('click',()=>activateHero(tab));tab.addEventListener('keydown',e=>{if(!['ArrowRight','ArrowLeft','Home','End'].includes(e.key))return;e.preventDefault();const n=e.key==='Home'?0:e.key==='End'?heroTabs.length-1:(i+(e.key==='ArrowRight'?1:-1)+heroTabs.length)%heroTabs.length;activateHero(heroTabs[n]);heroTabs[n].focus();});});
for(const group of document.querySelectorAll('[data-mechanism]')){const tabs=[...group.querySelectorAll('[role=tab]')];function activate(tab){for(const b of tabs){const active=b===tab;b.setAttribute('aria-selected',String(active));b.tabIndex=active?0:-1;document.getElementById(b.getAttribute('aria-controls')).hidden=!active;}}tabs.forEach((tab,i)=>{tab.addEventListener('click',()=>activate(tab));tab.addEventListener('keydown',e=>{if(!['ArrowRight','ArrowLeft','Home','End'].includes(e.key))return;e.preventDefault();const n=e.key==='Home'?0:e.key==='End'?tabs.length-1:(i+(e.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length;activate(tabs[n]);tabs[n].focus();});});}
// Progressive enhancement: content stays readable if scripting is disabled.
if('IntersectionObserver' in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){document.documentElement.classList.add('js-reveal');const observer=new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target);}}},{threshold:.06});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));}
